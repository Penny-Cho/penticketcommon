{
  "name": "@pentickets/common",
  "version": "1.0.2",
  "description": "",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "files": [
    "build/**/*"
  ],
  "scripts": {
    "clean": "del ./build/*",
    "build": "yarn clean && tsc",
    "pub": "git add . && git commit -m \"Updates\" && git push -u origin main && npm version patch && yarn build && npm publish "
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "del-cli": "^4.0.1",
    "typescript": "^4.6.3"
  }
}
